version: 1
skills:
  project-resolve:
    default_prompt: "Use $project-resolve for direct current-thread implementation of one ready Superpowers Project GitHub issue mirror with native /goal proof, TDD, verification, pushed branch, opened PR, and PR-ready handoff. Do not create worker threads from this skill; use $project-orchestrate for delegated worker worktree execution. Enforce flat canonical roots for spec -> plan -> issue; Milestone pages are index views, and nested canonical milestone artifact folders are drift unless explicitly generated index/view output. Activate native /goal proof, record a direct inline execution decision, execute with Superpowers TDD and verification, push the branch, open a PR that closes the exact issue, run collect-pr-ready-ledger.ps1 to assemble Temp Plus Evidence, pass the generated ledger to validate-pr-ready.ps1, complete the resolve goal at PR-ready, and hand off final integration to $project-merge. There is no hand-authored JSON requirement for normal runs. Do not merge, close issues, delete branches, or run final cleanup from this skill. At closeout, summarize the PR-ready result in chat, ask native question project_resolve_next_step with Project Merge, Resolve Another, Review First, and Stop options, then start the selected next skill in the same turn when tools and state allow it."
