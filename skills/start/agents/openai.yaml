interface:
  capabilities: [filesystem.read, shell, github, native.user-input]
  display_name: "Start"
  short_description: "Classify direct or governed work and own one outcome."
  default_prompt: "Use $project-truss:start for explicit or hard-trigger governed work; keep ordinary coding direct."
