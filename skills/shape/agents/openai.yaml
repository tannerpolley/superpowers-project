interface:
  capabilities: [filesystem.read, shell, github, native.user-input]
  display_name: "Shape"
  short_description: "Create the smallest native GitHub structure for one outcome."
  default_prompt: "Use $project-truss:shape to plan and create only the GitHub structure the governed outcome needs."
