interface:
  display_name: "Advanced User Input"
  short_description: "Use native user input for Project workflow gates, large option menus, and worker questions."
  default_prompt: >-
    Use $superpowers-project:advanced-user-input for request_user_input choices, branching, Custom answers,
    artifact review, or worker questions. Read SKILL.md and docs/superpowers/workflow-contract.yml.
    Never use debug mode to approve mutation.
