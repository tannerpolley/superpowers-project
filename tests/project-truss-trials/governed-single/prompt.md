Use `$project-truss:start` for this explicitly governed one-unit exercise. Use only the installed package at `{{PLUGIN_ROOT}}`; do not read a source checkout or call live GitHub.

From this project directory, run `{{PLUGIN_ROOT}}/scripts/project-truss.sh -Action Plan -RepoRoot . -RequestJson '{"explicit":true,"independent_units":1}'`, then run `{{PLUGIN_ROOT}}/scripts/project-truss.sh -Action Status -RepoRoot . -Repository example/project -Issue 201 -SnapshotPath snapshot.json`. Write `result.json` with `lane`, `layers`, `source`, `ready_frontier`, `blockers`, `next_action`, and `source_urls`, mapping the policy outputs faithfully. The fixture must retain its source link but expose no actionable Ready frontier. Return outcome `pass`, no blocker, and the result's source URLs.
