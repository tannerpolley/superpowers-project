Use `$project-truss:start` for this explicitly governed two-unit release exercise. Use only the installed package at `{{PLUGIN_ROOT}}`; do not read a source checkout, create a project board or local mirror, or call live GitHub.

From this project directory, run `{{PLUGIN_ROOT}}/scripts/project-truss.sh -Action Plan -RepoRoot . -RequestJson '{"explicit":true,"release_or_milestone":true,"independent_units":2}'`, then run `{{PLUGIN_ROOT}}/scripts/project-truss.sh -Action Status -RepoRoot . -Repository example/project -Issue 300 -SnapshotPath snapshot.json`. Write `result.json` with `lane`, `layers`, the fixture's `trial_dependency_edges` as `dependency_edges`, `source`, `ready_frontier`, `blockers`, `next_action`, and `source_urls`, mapping policy outputs faithfully. Preserve milestone, parent, leaves, dependencies, and all four source links while exposing no actionable Ready frontier. Return outcome `pass`, no blocker, and the result's source URLs.
