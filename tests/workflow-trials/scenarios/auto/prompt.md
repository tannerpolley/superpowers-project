# Auto Golden Trial

In the disposable repository, follow the source `initiate-workflow` and `implement-plan` contracts. Use the supplied trial authorization and workflow runtime. Implement the one authorized candidate by changing `result.txt` from `pending` to `complete`, record mutation, acceptance, verifier proof, and route completion. Do not ask the user, touch paths outside the disposable repository, use network services, push, or select another candidate.
