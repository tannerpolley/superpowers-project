# Auto Golden Trial

In the disposable repository, follow the source `initiate-workflow`, `implement-plan`, and `merge-changes` contracts. Use the supplied trial authorization and workflow runtime. Implement the one authorized outcome by changing `result.txt` from `pending` to `complete`, record mutation, acceptance, verifier proof, resolve `project_merge_final_health_gate` as Done, and complete the outcome. Do not ask the user, touch paths outside the disposable repository, use network services, push, or select another candidate.
