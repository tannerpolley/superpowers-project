version: 1
baseline_route_lines: 2741
target_reduction_percent: 30
capabilities:
  filesystem.read: Read repository files and contracts.
  filesystem.write: Write canonical project artifacts.
  shell: Run local validators and deterministic helpers.
  git: Inspect or mutate local Git state.
  github: Read or mutate GitHub state when separately authorized.
  native.user-input: Ask native structured user questions.
  subagents: Delegate bounded work to isolated workers.
  goals: Activate and inspect native Codex goals.
  browser: Preview companion artifacts when requested.
routes:
  align-project:
    required: [filesystem.read, shell, git]
    optional: [github, native.user-input]
    method_pairings: []
  audit-project:
    required: [filesystem.read, shell]
    optional: [native.user-input]
    method_pairings: []
  brainstorm-spec:
    required: [filesystem.read, filesystem.write, native.user-input]
    optional: [browser]
    method_pairings: [superpowers:brainstorming]
  companion-interface:
    required: [filesystem.read, filesystem.write]
    optional: [browser, native.user-input]
    method_pairings: []
  create-issues:
    required: [filesystem.read, filesystem.write, shell, github, native.user-input]
    optional: []
    method_pairings: []
  implement-plan:
    required: [filesystem.read, filesystem.write, shell, git, goals, native.user-input]
    optional: [subagents]
    method_pairings: [superpowers:executing-plans, superpowers:test-driven-development, superpowers:systematic-debugging, superpowers:verification-before-completion, superpowers:finishing-a-development-branch, superpowers:subagent-driven-development]
  initiate-workflow:
    required: [filesystem.read, shell, native.user-input]
    optional: []
    method_pairings: []
  loop-controller:
    required: [filesystem.read, filesystem.write, shell, native.user-input]
    optional: []
    method_pairings: []
  merge-changes:
    required: [filesystem.read, filesystem.write, shell, git, native.user-input]
    optional: [github]
    method_pairings: [superpowers:verification-before-completion, superpowers:finishing-a-development-branch]
  orchestrate-issues:
    required: [filesystem.read, filesystem.write, shell, git, subagents, native.user-input]
    optional: [github]
    method_pairings: [superpowers:subagent-driven-development, superpowers:using-git-worktrees, superpowers:test-driven-development, superpowers:verification-before-completion, superpowers:finishing-a-development-branch]
  resolve-issue:
    required: [filesystem.read, filesystem.write, shell, git, github, goals, native.user-input]
    optional: []
    method_pairings: [superpowers:using-git-worktrees, superpowers:executing-plans, superpowers:test-driven-development, superpowers:systematic-debugging, superpowers:verification-before-completion, superpowers:finishing-a-development-branch]
  setup-project:
    required: [filesystem.read, filesystem.write, shell, native.user-input]
    optional: [github]
    method_pairings: []
  write-plan:
    required: [filesystem.read, filesystem.write, shell, native.user-input]
    optional: [browser]
    method_pairings: [superpowers:writing-plans, superpowers:test-driven-development, superpowers:systematic-debugging, superpowers:verification-before-completion]
