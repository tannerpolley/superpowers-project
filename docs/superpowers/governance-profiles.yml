version: 1
profiles:
  manual:
    interactive: true
    max_candidates_per_iteration: 1
    requires_continuation: true
    completion_claims: [candidate, route, project]
  auto:
    interactive: false
    max_candidates_per_iteration: 1
    requires_continuation: false
    completion_claims: [candidate, route]
  looping:
    interactive: false
    max_candidates_per_iteration: 1
    requires_continuation: true
    completion_claims: [candidate, iteration, project]
  trial.local:
    interactive: false
    max_candidates_per_iteration: 1
    requires_continuation: true
    completion_claims: [candidate, iteration]
