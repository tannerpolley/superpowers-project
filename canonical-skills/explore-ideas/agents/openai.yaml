version: 1
skills:
  explore-ideas:
    default_prompt: "Use $explore-ideas in Default mode to meticulously audit a repo idea, codebase area, workflow, architecture concern, code-health concern, feature area, or roadmap area and grill the user with native request_user_input questions without Plan mode or an Implement Plan step. Inline docs-grilling behavior for repo work instead of relying on a nested skill: inspect context docs, ADRs, milestones, issues, workflows, relevant code, current features, code health, organization, efficiencies, and code/docs/issue connections first; use mapping tools such as Carto or Graphify when they add signal; batch independent questions through request_user_input; keep asking until material decisions are answered or the user pauses; do not create issues, branches, PRs, GoalBuddy boards, native goals, merges, or implementation edits; choose an owning milestone or approved cross-cutting milestone folder, then prefer a tracked docs/milestones/<milestone>/ideas idea brief with a hidden explore_ideas_brief handoff. Treat docs/ideas as legacy only. Do not paste the full JSON handoff in the final chat unless explicitly requested."
