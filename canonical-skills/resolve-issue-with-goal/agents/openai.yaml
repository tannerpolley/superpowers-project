version: 1
skills:
  resolve-issue-with-goal:
    default_prompt: "Use $resolve-issue-with-goal to resolve one ready GitHub issue mirror under docs/superpowers/issues with a linked source plan under docs/superpowers/plans. Ask the native execution topology question before branch setup: open a worker worktree thread or resolve in the current thread; keep the main thread as orchestrator, reviewer, merge owner, issue close owner, native goal completion owner, and cleanup owner. Run bundled scripts with explicit -RepoRoot: prepare-execution Inspect, preflight, ApplySetup, native goal activation with get_goal proof, FinalizeSetup, validate-setup, Superpowers execution, premerge, merge, issue close, native goal completion, cleanup, and closeout. Do not create GoalBuddy boards, docs/goals, goal_board_path, or GoalBuddy state in the default execution model. Require structured native goal proof, source-plan verification receipts, PR closing evidence, issue closure, branch cleanup, and cleanup hook proof."
